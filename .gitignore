# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
data

# Environment files
.env

# go2rtc generated config (contains camera credentials)
go2rtc.yaml

# YOLO model weights — auto-downloaded by ultralytics on first run
yolov8n.pt

# Camera config lives in data/ (gitignored) — never commit real RTSP credentials
/cameras.json

# UI screenshot harness output (scripts/ui_screenshot.mjs)
ui-shots/
